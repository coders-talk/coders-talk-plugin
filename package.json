{
    "name": "coders-talk-plugin",
    "private": true,
    "type": "module",
    "description": "Claude Code plugin: send a session to Coders Talk as a draft Build.",
    "engines": {
        "node": ">=20"
    },
    "scripts": {
        "test": "node --test",
        "build": "node scripts/build.mjs",
        "test:binary": "node scripts/smoke.mjs"
    }
}
